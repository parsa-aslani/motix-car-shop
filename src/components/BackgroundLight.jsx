const BackgroundLight = ({ top, bottom, left, right }) => {
  return (
    <div
      className="background-light"
      style={{ top, bottom, left, right }}
    ></div>
  );
};
export default BackgroundLight;
